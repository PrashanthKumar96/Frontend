import React, { useState, useEffect } from 'react';
import axios from 'axios';
import ImageUpload from './ImageUpload';

export default function TravelExpense1() {
  const expData = {
    expId: '12997',
    amount: '',
    approved_date: '',
    category: '',
    comments: '',
    empId: '',
    image: null,
    mgrId: '',
    status: '',
    submit_date: '',
    arrvial_city: '',
    expenseDate: '',
  };

  const [formData, setFormData] = useState(expData);
  const [expIdImage, setExpIdImage] = useState(null);
  const [selectedImage, setSelectedImage] = useState(null);

  const handleInputChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  const handleImageChange = (event) => {
    setSelectedImage(event.target.files[0]);
  };

  const handleUpload = () => {
    const formData = new FormData();
    formData.append('file', selectedImage);

    axios
      .post('http://localhost:9090/employee/saveFile', formData)
      .then((response) => {
        setExpIdImage(response.data.imageId); // Assuming imageId is the expId
        console.log(response.data);
      })
      .catch((error) => {
        console.error(error);
      });
  };

  useEffect(() => {
    // When expIdImage changes, update formData
    if (expIdImage) {
      setFormData({ ...formData, expId: expIdImage });
    }
  }, [expIdImage]);

  const handleSubmit = (e) => {
    e.preventDefault();

    // Make sure that expIdImage is available before submitting
    if (expIdImage) {
      axios
        .post('http://localhost:9090/employee/createexpense', formData)
        .then((response) => {
          console.log(response);
        })
        .catch((error) => {
          console.error(error);
        });
    } else {
      console.error('Image upload is not complete, expIdImage is null.');
    }
  };

  return (
    <section data-cvs-id={1467} className="m-6 p-6">
      {/* Rest of your component JSX */}
    </section>
  );
}
